# Rename this file to .env

# OpenRouter (recommended). If set, the app defaults to OpenRouter and seeds
# ANTHROPIC_BASE_URL, ANTHROPIC_AUTH_TOKEN, and model aliases if missing.
OPENROUTER_API_KEY=

# Optional: direct Anthropic access
ANTHROPIC_API_KEY=

# Optional OpenRouter routing overrides
# ANTHROPIC_BASE_URL=https://openrouter.ai/api
# ANTHROPIC_AUTH_TOKEN=

# Optional: override default model aliases
ANTHROPIC_DEFAULT_OPUS_MODEL=z-ai/glm-4.6
ANTHROPIC_DEFAULT_SONNET_MODEL=z-ai/glm-4.6
ANTHROPIC_DEFAULT_HAIKU_MODEL=z-ai/glm-4.5-air

# This is the API key for the app itself
API_KEY=

# Rate limiting (requests per time window)
# Format: "N/second", "N/minute", "N/hour", "N/day"
RATE_LIMIT_CHAT=20/minute
RATE_LIMIT_DEFAULT=60/minute

# MCP Server Tokens (for GitHub, Notion, Intercom integrations)
# GitHub: personal access token with repo/issues permissions
GITHUB_TOKEN=
# Notion: internal integration token from https://www.notion.so/my-integrations
NOTION_TOKEN=
# Intercom: access token from Developer Hub
INTERCOM_ACCESS_TOKEN=
